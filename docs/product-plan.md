# 제품 기획안 (TODO — 아직 작성되지 않음)

> **이 문서는 스텁입니다.** `CLAUDE.md`와 `feature_list.json`이 이 문서의 존재를 전제로 작성되어 있지만
> 실제 기획 내용은 아직 채워지지 않았습니다. `canvas-crud`, `collab-canvas`, `ai-image-generation`,
> `node-graph`, `preview-3d`, `optimization-pass` feature를 시작하기 전에 반드시 채워야 합니다.

작성해야 할 내용:

## 1. 목적

- 이 프로젝트를 왜 만드는가 (개인 프로젝트 / 포트폴리오 / 실제 서비스 등)
- 어떤 문제를 해결하는가

## 2. 사용자 시나리오

- 누가 이 툴을 쓰는가
- 대표 사용 흐름 (예: 프롬프트 입력 → 이미지 생성 → 캔버스 합성 → 노드 그래프 분기 → 3D 프리뷰)
- `docs/acceptance-criteria.md`의 시나리오 1~11과 매핑되어야 함

## 3. 화면 구성

- 주요 화면/패널 목록 (캔버스 에디터, 노드 그래프, 3D 프리뷰, 프롬프트 입력창 등)
- 화면 간 전환 방식

## 4. 지원 직무와의 연결

- 이 프로젝트가 어떤 역량을 보여주기 위한 것인지 (예: 실시간 협업 시스템 설계, WebSocket 프로토콜 구현, 3D 렌더링 최적화)

## 5. 범위 조정 옵션

- 시간이 부족할 경우 잘라낼 수 있는 기능과 그 우선순위
- 반드시 남겨야 하는 핵심 기능

## 6. 최적화 체크리스트 (9번 섹션)

- `optimization-pass` feature가 참조하는 Three.js 최적화 우선순위 항목 목록
